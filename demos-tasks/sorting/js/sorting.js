var single_feature_trial = {
    type: jsPsychAudioTokens,
    ratingtype: 'cluster',
    stimuli: sounds,
    force_listen: false,
    loop: true,
    prompt: '<p>Arrange these sounds in the way that seems most appropriate to you.</p>'
}

timeline.push(single_feature_trial);